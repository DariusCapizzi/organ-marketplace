import { organCost } from 'online-marketplace/helpers/organ-cost';
import { module, test } from 'qunit';

module('Unit | Helper | organ cost');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = organCost([42]);
  assert.ok(result);
});
